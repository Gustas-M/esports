@using System.Text.Json

@code {
    public static string ConvertToken(string jsonToken)
    {
        var jsonObject = JsonSerializer.Deserialize<JsonElement>(jsonToken);
        string tokenVal = jsonObject.GetProperty("accessToken").GetString();

        return tokenVal;
    }
}
